{% extends layout_frontend %}
{% block title %}{{opportunite.title}} | UCM{% endblock %}
{% block content %}
<div class="main-container">
    <!-- hidden Title H1 -->
    <h1 class="hide">Opportunité</h1>

    <!-- big slider banner home -->
    <section class="banner banner-training">
        <div class="banner-container">
            <figure class="bigImg">
                <img src="{{asset("uploads/opportunite/")~ opportunite.uuid~"/1073-"~ opportunite.path}}" '>
            </figure>
            <div class="desc">
                
            </div>
        </div>
    </section> <!-- /big slider banner home -->
    
    <!-- breadcrumb -->
    <nav aria-label="breadcrumb" class="breadcrumb-bloc container">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/"><i class="iconucm-home"></i></a></li>
            <li class="breadcrumb-item active" aria-current="page"><a href="/" title="Actualité">Actualité</a></li>
            <li class="breadcrumb-item active" aria-current="page"><span>{{opportunite.title}}</span></li>
        </ol>
    </nav><!-- /breadcrumb -->

    
    <div class="main-contentPage actus-container">
        <div class="container">
            <!-- aside Right -->
            <aside class="asideRight asideSticky">
                <!-- aside Right actuality-->
             {{ include('frontend/layoutactus/_opportunite.html.twig') }} 
            </aside>
            <!-- /aside Right -->

            <!-- main Bloc -->
            <article class="mainBloc mainBloc-training" id="mainBloc">
                <!-- main content training -->
                <section class="trainingDetail-bloc actuDetail-bloc">
                    <div class="main-bloc">
                        <div class="top-titre">
                            <div class="title-group">
                                <p class="sup-title">Opportunité</p>
                                <h2 class="title-bloc">{{opportunite.title}}</h2>
                                <!-- <h2 class="title-bloc">{{opportunite.description}}</h2> -->
                            </div>
                        </div>
                        
                        <div class="date-pub">
                            Publié le: {{ opportunite.createdAt | date('d/m/Y') }}
                        </div>
                        
                        <div class="tag-list">
                            {% if(derniereOpportunite | length > 0) %}
                            {% for opportunite in derniereOpportunite %}
                                <div class="item-actu">
                                    {%  set param = null %}
                                    {% if opportunite.slug is not empty %}
                                        {%  set param = path('details_actualite', { slug: opportunite.slug })  %}
                                    {% endif %}
                                    <a href="{{ param }}" title="{{ opportunite.title }}">{{ opportunite.title }}</a>
                                </div>
                            {% endfor %}
                        {% endif %}
                        </div>

                        <!-- tabs and swipe system -->
                        <section class="actus-content richText">
                            {{opportunite.detail|raw}}
                        </section>
                        <!-- /tabs and swipe system -->
                    </div>
                </section>
            </article> <!-- /main Bloc -->
        </div>

        <section class="offres-bloc">
            <figure class="backgroundImg">
                <img src="{{ asset('/assets/images/data/training/banner-economics-management.png') }}" alt="Training Background">
            </figure>
            <div class="content-text">
                <div class="container">
                    <h2 class="offres-title">Intéressé ?</h2>
                    <h3 class="sub-title">Alors, n'hésitez pas à nous envoyer votre dossier, à bientôt !</h3>
                    <p class="group-btns">
                        <a class="btn-primary" href="/concours/candidature" title="Candidature spontanée">Candidature spontanée</a>
                        <a class="btn-primary" href="/formation" title="Consultez nos offres">Consultez nos offres</a>
                    </p>
                </div>
            </div>
        </section>
    </div>
</div>
{% endblock %}
